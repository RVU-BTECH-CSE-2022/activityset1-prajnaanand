#include <stdio.h>
int main()
{
  float length = 10.0;
  float breadth = 20.0;
  float area = length * breadth;
  printf("The area of the rectangle with length = %f and breadth %f is %f\n",length, breadth, area);
  return 0;
}